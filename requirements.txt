# Create the virtual environment inside this project (for example .venv/) so
# dependencies and their caches stay portable with WhisperDock's data folders.
# Use Python 3.10-3.13: current PyTorch wheels may not yet support newer Python.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
# Project-local FFmpeg binary for decoding browser-recorded and common media
# formats without requiring a system-wide FFmpeg installation.
imageio-ffmpeg>=0.5

# Default OpenAI Whisper engine.
openai-whisper>=20240930
torch>=2.2

# Optional community-model adapter. Required for Hugging Face folders such as
# panlr/whisper-finetune-teochew; harmless for built-in OpenAI models.
transformers>=4.45
accelerate>=0.26
huggingface-hub>=0.26
safetensors>=0.4
