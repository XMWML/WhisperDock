[project]
name = "whisperdock"
version = "0.1.0"
description = "Portable local Whisper transcription workstation"
requires-python = ">=3.10,<3.14"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py310"
